# AutoLinker 5.1.2 静默 MCP 版改动记录

## 版本信息
- **原始版本**: AutoLinker 5.1.2 官方版
- **修改版本**: AutoLinker 5.1.2 静默 MCP 版
- **修改日期**: 2026-08-27

---

## 改动概述

本版本基于官方 AutoLinker 5.1.2 进行修改，实现了**纯本地化运行**，移除了所有网络行为，同时保留了完整的 **Local MCP** 功能。

### 保留的功能
- ✅ 本地 MCP 服务器（127.0.0.1:19207）
- ✅ 源码读写工具（read_file, write_file, edit_file 等）
- ✅ 编译工具（compile_e_project, compile_c_project 等）
- ✅ e-packager 本地调用功能
- ✅ IDE 集成功能

### 移除的功能
- ❌ AI Chat UI 界面（完全禁用）
- ❌ IDE 右键菜单
- ❌ AddIn 菜单项
- ❌ 版本检查更新
- ❌ GameAnalytics 遥测
- ❌ 网络请求（WinINet）
- ❌ Tavily 搜索
- ❌ Web 文档获取
- ❌ 外链 URL

---

## 详细改动清单

### 1. AIChatFeature.cpp

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 105 | 新增 | `constexpr bool kAIChatIdeTabsEnabled = false;` | 完全禁用 AI Chat UI 标签页 |
| 118-122 | 修改 | 清空所有外链 URL（kChatMcpGuideUrl 等为空字符串） | 移除引导用户访问外部网站的链接 |
| 307-311 | 修改 | `FneCheckNewVersion` 函数体为空 | 禁用版本检查线程，避免网络请求 |
| 728-736 | 新增 | 添加 `LoadPublicToolAutoAllowWrites()` 函数 | 支持静默 MCP 模式的工具调用控制 |
| 1379-1386 | 新增 | 添加 `BuildPublicToolWriteDisabledResult()` 函数 | 返回公共工具写入被禁用的错误信息 |
| 6005-6017 | 修改 | 添加 publicToolCall 检查逻辑 | 区分本地 MCP 和公共调用的权限控制 |
| 6801 | 修复 | 函数调用签名从 6 参数改为 4 参数 | 保持与 5.1.2 原始版本的兼容性 |

### 2. AIChatToolingInternal.h

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 19 | 新增 | `bool publicToolCall = false;` | 在 ToolExecutionRequest 结构体中添加标识字段 |

### 3. AIChatTooling.h

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 16 | 修改 | 添加 `bool publicToolCall = false` 参数 | 支持区分本地和公共工具调用 |

### 4. AutoLinkerMenu.cpp

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 14 | 新增 | `constexpr bool kAutoLinkerUiMenusEnabled = false;` | 完全禁用 IDE 右键菜单功能 |

### 5. AutoLinker.cpp

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 163 | 修改 | `kEntries = {}`（空数组） | 移除所有 AddIn 菜单项 |
| 310 | 修改 | 版本检查函数输出"已禁用"日志 | 确认版本检查已禁用 |
| 391 | 修改 | 初始化日志输出"启动版本检查已禁用" | 确认启动时不检查版本 |

### 6. AIChatTooling.cpp

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 476 | 修改 | `run_powershell_command` 返回错误 | 禁用 PowerShell 命令执行，避免 IDE 宿主进程不稳定 |
| 487 | 修改 | `search_web_tavily` 返回错误 | 禁用 Tavily 网络搜索功能 |

### 7. EPackagerIntegration.cpp

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 638 | 修改 | 自动更新检查返回错误 | 禁用 e-packager 自动下载功能 |

### 8. GameAnalyticsClient.cpp

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 847 | 修改 | 设置 `REMOTE_CONFIG_DISABLED` | 禁用远程配置获取 |
| 874 | 修改 | 预设禁用配置 | 确保遥测数据不发送 |

### 9. AIChatFeature.cpp - 弹窗确认处理

| 行号 | 改动类型 | 改动内容 | 原因 |
|------|----------|----------|------|
| 6006-6021 | 修改 | 静默 MCP 模式工具执行逻辑 | 所有本地调用（`publicToolCall=false`）的写入工具直接放行，不弹确认对话框 |
| 6006-6021 | 保留 | 公共调用（`publicToolCall=true`）仍需检查自动写入开关 | 安全限制，防止外部恶意调用 |
| 6019 | 新增 | 日志输出"silent mode: auto allow write tool" | 便于调试和追踪 |

**改动说明**：
- 本地 MCP 调用（来自外部 Agent 的 `edit_file`、`multi_edit_file` 等写入工具）：直接执行，不弹确认框
- 公共调用：保持原有安全检查，只有开启自动写入开关才放行

---

## 技术实现细节

### 1. UI 完全禁用

通过设置编译时常量 `kAIChatIdeTabsEnabled = false` 和 `kAutoLinkerUiMenusEnabled = false`，在编译期禁用所有 UI 相关代码路径。

### 2. 网络请求禁用

- `WinINetUtil.cpp`: 保留代码但通过配置禁用所有 HTTP 调用
- `TavilyClient.cpp`: 所有搜索功能直接返回错误
- `WebDocumentClient.cpp`: Web 文档获取返回错误

### 3. Local MCP 保留

Local MCP 服务器（127.0.0.1:19207）完全保留，提供了以下本地工具：
- `read_file` / `write_file` / `edit_file`: 文件操作
- `compile_e_project`: 易语言项目编译
- `compile_c_project`: C 项目编译
- 其他本地文件系统工具

### 4. 工具调用权限控制

通过 `publicToolCall` 标志区分：
- **本地调用**（来自 MCP 客户端）：允许执行文件操作
- **公共调用**（来自 UI）：需要额外确认（静默模式下直接拒绝写入）

---

## 编译配置

### Visual Studio 项目文件改动

| 文件 | 改动内容 |
|------|----------|
| AutoLinker.vcxproj | 将 `PlatformToolset` 从 `v145` 改为 `v143`，以兼容 Visual Studio 2022 |

### GitHub Actions 工作流

`.github/workflows/build-fne.yml` 配置：
- 触发方式：推送到 main 分支 或 手动触发
- 构建配置：`fne_release` / `Win32`
- 产物：AutoLinker.fne

---

## 验证方法

### 1. 功能验证

启动易语言 IDE 后：
1. 检查是否有 AI Chat 标签页 → **不应存在**
2. 检查右键菜单是否有 AutoLinker 选项 → **不应存在**
3. 检查 AddIn 菜单是否有 AutoLinker → **不应存在**
4. 测试 Local MCP 连接（127.0.0.1:19207）→ **应正常连接**

### 2. 网络行为验证

使用网络抓包工具（如 Wireshark）监控：
1. 启动 IDE 时不应有到外部服务器的 HTTP 请求
2. 不应有 GameAnalytics 相关流量
3. 不应有版本检查请求

---

## 已知限制

1. **无 UI 配置界面**：所有配置需通过手动编辑配置文件
2. **Tavily 搜索不可用**：需要网络搜索时请使用其他 MCP 工具
3. **PowerShell 命令禁用**：安全限制，避免潜在的安全风险

---

## 相关文件

- 源码包：`AutoLinker-5.1.2-纯本地版-源码.zip`
- 编译产物：`AutoLinker-5.1.2-纯本地版-成品.zip`
- GitHub 仓库：https://github.com/giaosang8888-cmd/AutoLinker-5.1.2-Local

---

## 联系与支持

如有问题或建议，请通过 GitHub Issues 反馈。
