# AutoLinker 5.1.2 纯本地版 - 编译指南

## 文件说明

本压缩包包含 **AutoLinker 5.1.2 纯本地版** 的完整源代码，已修改为：
- ✅ 完全禁用所有网络请求
- ✅ 禁用遥测/分析功能
- ✅ 禁用外部链接
- ✅ **MCP 本地服务完整保留**

## 编译环境要求

### 必须安装
- **Visual Studio 2022** 或 **Visual Studio 2017**（社区版免费）
- 安装时选择 **"使用 C++ 的桌面开发"** 工作负载
- 确保安装 **MFC for x86/x64** 组件

### 下载地址
- VS 2022: https://visualstudio.microsoft.com/downloads/
- VS 2017: https://my.visualstudio.com/Downloads?q=visual%20studio%202017

## 编译步骤

### 方法 1：使用 Visual Studio 界面（推荐）

1. 解压本压缩包
2. 进入目录 `AutoLinker-5.1.2-纯本地版`
3. 双击打开 `AutoLinker.sln`
4. 在顶部工具栏设置：
   - **配置(C):** `fne_release`
   - **平台(P):** `Win32`
5. 菜单 → `生成` → `重新生成解决方案`（或按 `Ctrl+Shift+B`）
6. 等待编译完成（约 5-15 分钟）

### 方法 2：使用命令行

```powershell
# 进入源码目录
cd AutoLinker-5.1.2-纯本地版

# 使用 MSBuild 编译
MSBuild.exe AutoLinker.sln /p:Configuration=fne_release /p:Platform=Win32 /t:Rebuild
```

## 输出文件

编译成功后，`.fne` 文件位于：

```
x64\Release\fne_release\AutoLinker.fne
```

或（取决于配置）

```
Win32\Release\AutoLinker.fne
```

## 常见问题

### Q: 提示缺少 MFC
**A:** 重新安装 Visual Studio，确保勾选 **"MFC for x86/x64"** 组件

### Q: 工具集版本错误
**A:** 项目使用 `v145`（VS 2017）工具集。如果使用 VS 2022，需要将项目文件中的 `v145` 改为 `v143`

### Q: 编译卡住不动
**A:** 首次编译需要较长时间（约 10-15 分钟），请耐心等待

## 改动说明

详细改动内容请查看 `CHANGES.md` 文件。

---

**修改者:** Claude (AI Assistant)
**日期:** 2026-08-27
**版本:** 5.1.2-Local
